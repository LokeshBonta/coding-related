#ifndef LCS_HPP
#define LCS_HPP

#include <string>
#include <vector>
#include <algorithm>

namespace DP {
    int longestCommonSubsequence(const std::string& text1, const std::string& text2);
}

#endif // LCS_HPP
