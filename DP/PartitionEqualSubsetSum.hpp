#ifndef PARTITION_EQUAL_SUBSET_SUM_HPP
#define PARTITION_EQUAL_SUBSET_SUM_HPP

#include <vector>
#include <numeric>

namespace DP {
    bool canPartition(const std::vector<int>& nums);
}

#endif // PARTITION_EQUAL_SUBSET_SUM_HPP
